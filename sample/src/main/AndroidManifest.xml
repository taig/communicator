<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  package="com.taig.communicator.sample">

	<uses-sdk android:minSdkVersion="11" />

	<uses-permission android:name="android.permission.INTERNET" />

	<application android:label="@string/app_name">

		<activity android:label="@string/app_name"
				  android:name=".app.Main">

			<intent-filter>

				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />

			</intent-filter>

		</activity>

		<activity android:name=".app.SimpleRequest" />

		<activity android:name=".app.Events" />

		<activity android:name=".app.CustomResultParser" />

		<activity android:name=".app.FormSend" />

	</application>

</manifest>