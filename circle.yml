machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t taig/communicator .

test:
  override:
    - docker run taig/communicator

#general:
#  artifacts:
#    - target/scala-2.11/coverage-report
#
#test:
#  override:
#    - sbt ";clean;coverage;test;coverageReport;tut;coverageAggregate"
#  post:
#    - pip install --user codecov && codecov